action (findRace) {
  description (Given the name of a DnD 5e race return information on that race.)
  type (Search)
  collect {
    input (name) {
      type (raceName)
      min (Required)
      max (One)
    }
  }
  output (Race)
}
